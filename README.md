# Telegram-бот с AI-ассистентом и WebApp чатом для службы поддержки "YOUR COMPANY"


Это MVP (Minimum Viable Product) проекта, демонстрирующий создание комплексного решения для клиентской поддержки автопрокатной компании "YOUR COMPANY" в Белграде. Система включает Telegram-бота для инициации диалога, веб-приложение (WebApp) для непосредственного общения клиента с AI-ассистентом или оператором, и бэкенд на FastAPI.


## 🚀 Ключевые возможности

1.  **AI-ассистент на базе локальной LLM:**
    *   Используется модель **Gemma 3 12B** (или аналогичная, доступная через OpenAI-совместимый API, например, через Ollama или vLLM).
    *   Отвечает на вопросы клиентов **исключительно** на основе предоставленного текстового контекста (`company_context.txt`), минимизируя "галлюцинации".
    *   Реализован протокол "Contact Manager": если AI не может найти ответ, он предлагает связаться с оператором.
    *   Распознает типы вопросов, на которые контекст, скорее всего, не содержит ответов (например, проверка доступности конкретных авто на конкретные даты в реальном времени).

2.  **Интеграция с Telegram:**
    *   **Точка входа для пользователя:** Клиент начинает общение через Telegram-бота, который предлагает открыть WebApp-чат.
    *   **Группа для менеджеров:**
        *   При запросе оператора или если AI не справляется, создается отдельный топик в Telegram-группе менеджеров.
        *   Менеджеры могут общаться с клиентом прямо из Telegram, отвечая в соответствующий топик.
        *   Уведомления о новых запросах и возможность "закрыть" чат.

3.  **WebApp чат для клиента:**
    *   **Real-time общение:** Построен с использованием FastAPI и WebSockets.
    *   **Аутентификация:** Безопасная аутентификация пользователя через `Telegram.WebApp.initData`.
    *   **История сообщений:** Отображение истории переписки.
    *   **Интерфейс:**
        *   Отправка текстовых сообщений.
        *   Загрузка файлов (изображения, документы) с хранением в MinIO.
        *   Кнопки для обратной связи ("Я доволен ответом") и запроса оператора.
    *   **Адаптивный дизайн:** 
        *   Интерфейс оптимизирован для десктопов, iPhone и Android устройств.
        *   Поддержка различных размеров экранов и ориентаций.
        *   Временное решение - планируется передача фронтенда специалистам для полной переработки UI/UX.
4.  **Бэкенд и инфраструктура:**
    *   **FastAPI:** Для API-эндпоинтов, обслуживания WebApp и WebSocket-соединений.
    *   **MongoDB (Motor):** Асинхронная NoSQL база данных для хранения информации о пользователях, чатах, сообщениях.
    *   **MinIO:** S3-совместимое хранилище для медиафайлов.
    *   **Асинхронность:** Широкое использование `async/await` для высокой производительности.
    *   **Конфигурация:** Через `.env` файлы.
    *   **Логирование:** С использованием `loguru`.

## 🛠 Технологический стек

*   **Бэкенд:** Python 3.10+, FastAPI, Uvicorn
*   **Telegram Бот:** Aiogram 3.x
*   **База данных:** MongoDB (с асинхронным драйвером Motor)
*   **AI:** Локальная LLM (например, Gemma) через OpenAI-совместимый API, библиотека `openai` для взаимодействия.
*   **Хранилище файлов:** MinIO (S3-совместимое)
*   **Real-time:** WebSockets
*   **Фронтенд:** HTML, CSS, JavaScript (интегрировано с Telegram WebApp API)
*   **Окружение:** Docker (рекомендуется для БД и MinIO), Ngrok (для демонстрации)

## 📂 Структура проекта (основные компоненты)

```
.
├── ai_integration.py       # Логика взаимодействия с AI моделью
├── company_context.txt     # Контекст (база знаний) для AI
├── config.py               # Загрузка конфигурации и настройка логгера
├── database.py             # Функции для работы с MongoDB
├── main.py                 # Основное FastAPI приложение (HTTP, WebSockets)
├── minio_storage.py        # Класс для работы с MinIO
├── models.py               # Pydantic модели данных
├── requirements.txt        # Зависимости Python
├── static/                 # Статические файлы фронтенда (CSS, JS)
│   └── script.js
│   └── style.css
├── templates/              # HTML шаблоны (Jinja2)
│   └── chat.html
├── telegram_bot.py         # Логика Telegram-бота (aiogram)
├── utils.py                # Вспомогательные утилиты
├── websocket_manager.py    # Менеджер WebSocket соединений
└── .env.example            # Пример файла окружения
```

## ⚙️ Как это работает (упрощенный флоу)

1.  **Инициация:** Пользователь находит Telegram-бота и нажимает кнопку "Открыть чат". Открывается Telegram WebApp (`chat.html`).
2.  **Аутентификация WebApp:** JavaScript в `chat.html` использует `Telegram.WebApp.initData` для безопасной передачи данных пользователя на бэкенд при установке WebSocket-соединения. Бэкенд верифицирует `initData`.
3.  **Первое сообщение от клиента:**
    *   Клиент пишет сообщение в WebApp.
    *   Сообщение отправляется через WebSocket на FastAPI-сервер.
    *   Сервер передает вопрос AI-ассистенту (`ai_integration.py`).
    *   AI генерирует ответ на основе `company_context.txt`.
    *   Ответ AI отправляется клиенту через WebSocket.
4.  **Запрос оператора:**
    *   Если AI не может ответить или клиент нажимает "Позвать оператора":
        *   Создается новый топик в Telegram-группе менеджеров.
        *   В топик отправляется история переписки и информация о клиенте.
    *   Менеджер отвечает в этот топик.
    *   Сообщение менеджера перехватывается `telegram_bot.py` и через WebSocket (`websocket_manager.py`) отправляется клиенту в WebApp.
5.  **Обмен сообщениями с оператором:** Продолжается через связку Telegram (для менеджера) <-> FastAPI <-> WebApp (для клиента).
6.  **Загрузка файлов:** Клиент может прикрепить файл в WebApp. Файл загружается на FastAPI-сервер (`/upload`), сохраняется в MinIO, и информация о нем (ссылка, тип) отправляется как сообщение в чат.
7.  **Завершение чата:** Клиент может нажать "Я доволен ответом" или менеджер может закрыть чат из Telegram. Статус чата обновляется в БД.

## 🚀 Запуск и настройка

1.  **Клонировать репозиторий:**
    ```bash
    git clone <your-repo-url>
    cd <repo-name>
    ```
2.  **Настроить окружение:**
    *   Создайте виртуальное окружение и активируйте его:
        ```bash
        python -m venv venv
        source venv/bin/activate # для Linux/macOS
        # venv\Scripts\activate # для Windows
        ```
    *   Установите зависимости:
        ```bash
        pip install -r requirements.txt
        ```
3.  **Настроить `.env` файл:**
    *   Скопируйте `.env.example` в `.env`.
    *   Заполните необходимые переменные:
        *   `TELEGRAM_BOT_TOKEN`: Токен вашего Telegram-бота.
        *   `MONGO_CONNECTION_STRING`: Строка подключения к MongoDB.
        *   `DATABASE_NAME`: Имя базы данных MongoDB.
        *   `MANAGER_GROUP_CHAT_ID`: ID Telegram-группы для менеджеров (должна быть супергруппой с включенными топиками/форумом).
        *   `ADMIN_USER_ID`: Telegram ID администратора бота (для первоначального добавления менеджеров).
        *   `OPENAI_API_BASE`: URL вашего локального OpenAI-совместимого сервера (например, `http://localhost:11434/v1` для Ollama).
        *   `OPENAI_MODEL_NAME`: Имя модели, которую будет использовать сервер (например, `gemma:2b` для Ollama, если используете модель Gemma).
        *   `OPENAI_API_KEY`: API ключ для сервера (часто можно оставить `ollama` или пустым для локальных серверов).
        *   `MINIO_ENDPOINT`: Адрес вашего MinIO сервера (например, `localhost:9000`).
        *   `MINIO_ACCESS_KEY`: Ключ доступа MinIO.
        *   `MINIO_SECRET_KEY`: Секретный ключ MinIO.
        *   `WEB_APP_URL`: Публичный URL, по которому будет доступен ваш FastAPI сервер (например, URL от Ngrok).

4.  **Запустить сервисы:**
    *   **MongoDB:** Убедитесь, что у вас запущен экземпляр MongoDB. Можно использовать Docker:
        ```bash
        docker run -d -p 27017:27017 --name mongo mongo
        ```
    *   **MinIO:** Убедитесь, что у вас запущен экземпляр MinIO. Можно использовать Docker:
        ```bash
        docker run -d -p 9000:9000 -p 9001:9001 --name minio \
          -e "MINIO_ROOT_USER=YOUR_ACCESS_KEY" \
          -e "MINIO_ROOT_PASSWORD=YOUR_SECRET_KEY" \
          minio/minio server /data --console-address ":9001"
        ```
        Не забудьте создать бакет `chat` в MinIO (или он создастся автоматически при первом запуске).
    *   **Локальный LLM сервер:** Запустите ваш сервер LLM (например, Ollama). Убедитесь, что нужная модель (Gemma) загружена и доступна.
        ```bash
        ollama serve
        ollama pull gemma:2b # или gemma:7b, gemma2:9b и т.д.
        ```

5.  **Запустить FastAPI приложение:**
    ```bash
    uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    ```
6.  **Настроить публичный доступ (для Telegram WebApp):**
    *   Используйте Ngrok или аналогичный сервис для получения публичного HTTPS URL для вашего локального сервера:
        ```bash
        ngrok http 8000
        ```
    *   Скопируйте HTTPS URL из вывода Ngrok (например, `https://xxxx-yyy.ngrok.io`) и вставьте его в `WEB_APP_URL` в вашем `.env` файле. Перезапустите FastAPI приложение, если меняли `.env`.
    *   Установите этот URL как WebApp URL для вашего Telegram-бота через BotFather (`/setmenubutton`).

7.  **Добавить первого менеджера (если нужно):**
    *   Отправьте команду `/addmanager <ADMIN_USER_ID> AdminName` боту от имени пользователя, чей ID указан в `ADMIN_USER_ID` в `.env`. Это позволит администратору использовать команды управления менеджерами.
    *   Затем администратор может добавлять других менеджеров командой `/addmanager <MANAGER_TELEGRAM_ID> ManagerName`.

## ✨ Перспективы развития

*   **Интеграция RAG (Retrieval Augmented Generation):** Для более точных ответов AI на основе большого объема документов компании (контракты, FAQ, условия).
*   **Административный интерфейс:** Для управления контекстом AI, просмотра статистики, управления менеджерами.
*   **Улучшение AI:** Файн-тюнинг модели под специфику компании, более сложные сценарии диалогов.
*   **Полная Docker-композиция:** Для упрощения развертывания всего стека одной командой.
*   **CI/CD:** Автоматизация сборки, тестирования и развертывания.
*   **Расширенная аналитика:** Сбор и анализ данных о запросах клиентов, эффективности AI и операторов.

## Демонстрация
![Демонстрация работы](demo4.gif)
